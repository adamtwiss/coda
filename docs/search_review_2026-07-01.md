# Search Deep-Review Synthesis — 2026-07-01

Input: 62 adversarially-verified findings (17 refuted items already excluded) from 16 finder agents across 8 clusters. After cross-cluster dedupe: **41 distinct items** (7 root causes appeared in 2–3 clusters each and are merged below, with both citations noted).

**Dedupe map:** TT-bound refinement of pruning eval (whole-node #2 + tt #9 + skeleton #25); ProbCut stale cont-hist stack (whole-node #4 + ordering #32); corrhist pre-correction training (skeleton #31 + ordering #45); ZW-narrowing inverted window (tt #12 + skeleton #28); TM stale-elapsed (skeleton #22 + driver #55); aborted-iteration discard / root-move list (skeleton #30 + driver #56/#57); 50-move checkmate precedence (skeleton ×2 + qsearch #37); QS double-SEE (ordering #40 + qsearch #51); mate-only bounds admitting TB scores (whole-node #3 + move-loop #6 + tt #17); in-check eval at ply caps (skeleton #24 + qsearch #35, plus QS ply-cap mate-band #27); best_move without alpha gate (tt #16 + qsearch #47); promo history routing subsumes the quiet-malus off-by-one (ordering #44 + move-loop #5).

A recurring theme: a large fraction of the confirmed correctness items are **invisible at OB** (TB-enabled, Threads>1, ponder, ARM, `go nodes`) and only bite in the lichess/CCRL deployment regime. They are grouped and flagged accordingly.

---

## P0 — Likely bugs (traced + confirmed)

### 1. Non-PV first-move child searched with `cut_node=false` instead of `!cut_node`
`src/search.rs:4595` (convention comment at :2949). The "first move: always full window" branch hardcodes the child's cut_node to false; at a non-PV all-node the first child is an expected CUT node and all 6 references pass `!cutNode`. This mislabels the entire leftmost spine of every all-node subtree and directly disables NMP (`&& cut_node`, :3656 — the merged #864 Reckless gate means NMP can *never* fire there), IIR (:3752), the TT-cutoff node-type guard (:3196 — legitimate LOWER-bound cutoffs rejected), and degrades the SE negative-ext branch (:4145). Dates unchanged to the GoChess translation; a 2026-06-26 audit passed the line via misread, not deliberate choice.
**Change:** pass `if is_pv { false } else { !cut_node }` in the first-move branch. **Test:** [-2,1] per bug-fix bounds policy (or [0,3] as feature-enable). Bench will shift substantially — **retune-on-branch candidate if raw SPRT is flat** (all SPSA calibration happened with the mislabel). Novelty: new; no prior test of this line (SE-site cut_node H0s #1201/#2415 are a different, no-consensus site).

### 2. ProbCut child searches run with a stale `moved_piece_stack`/`moved_to_stack` slot — cont-hist corruption
`src/search.rs:3806-3867` (loop), readers/writers at :3883, :4555/:4680/:4726. (Merged: whole-node #4 + ordering #32.) The ProbCut loop is the **only recursion site** that never writes the cont-hist context stack (writers: :3628, :3687, :4157-4159, :5132, :5343). The child at ply+1 reads whatever unrelated sibling move last occupied the slot, feeds it to MovePicker ordering, and on every beta cutoff in the verification subtree **writes** bonuses/maluses into `cont_hist[stale_piece][stale_to][...]` (offsets 1/2/4/6), polluting the shared table for the rest of the search. SF/Reckless both set the stack in their ProbCut do_move. Exact analog of the C3 NMP-sentinel bug (#640/#668, merged as confident-correctness).
**Change:** capture the colored mover via `board.piece_at(move_from(mv))` *before* make_move (the loop currently only computes piece_type), then write `go_piece(..)`/`move_to(mv)` into the slot after make_move succeeds; no restore needed. **Test:** [-2,1] confident-correctness (NMP-sentinel precedent H0'd −1.3/−1.6 standalone and was merged anyway; expect ~0 to small positive). Novelty: new.

### 3. ZW TT window narrowing not gated on `halfmove_ok` — inverted window (alpha > beta) at halfmove 89–99, re-stored at full depth
`src/search.rs:3256-3268` (mutation), :3270 (gated collapse-return), :3176 (`halfmove_ok`, default 89). (Merged: tt #12 + skeleton #28.) At zero-window nodes the narrowing can only ever fully collapse the window (LOWER with `tt_score > alpha` implies `tt_score >= beta`); the collapse-*return* is halfmove-gated (P2, #628) but the alpha/beta *mutation* is not. At halfmove ≥ 89 with a matching deep TT bound, the node proceeds through the whole move loop with alpha ≥ beta: first child gets a literally inverted window, PVS re-search conditions become unsatisfiable, razoring/futility fire against the inflated alpha, and the node TT-stores a full-depth, current-generation bound derived from this degenerate search — re-stamping exactly the stale near-50mr bound P2 exists to distrust. The in-code comment "it only biases the search" (:3173-3175) is mathematically wrong for the only window shape narrowing applies to. Fires in shuffle/conversion endgames (the documented conversion-failure regime).
**Change:** wrap the narrowing `match` in `if halfmove_ok { ... }`. **Test:** [-2,1] correctness; LTC is the diagnostic frame (cf. #2418: STC inert, LTC H1 +0.7). Bench changes. Novelty: new (#628 gated returns only).

### 4. `max_nodes` limit never sets the stop flag — partial 0-scores TT/history-stored on node-limited searches
`src/search.rs:1116-1118` vs time branches :1126-1128/:1150-1152; guards keyed off the flag at :4838 (TT store), :4886 (corrhist), :4608 (unwind). The node-limit branch of `should_stop()` returns true without `self.stop.store(true)`, so on `go nodes N` every negamax entry returns 0 but ancestors complete their epilogues, storing polluted best_score/best_move into the TT at full claimed depth (persisting across the game) and firing fake beta-cutoff history bonuses when beta ≤ 0. Helpers also never see the limit. No effect on time-based play or bench.
**Change:** mirror the time branches — `self.stop.store(true, Ordering::Relaxed); return true;`. **Test:** [-2,1] non-regression (behavior-neutral for timed play). Affects `go nodes` / fixed-node / node-odds use only. Novelty: new.

### 5. SMP result selection picks a move, not a thread: vote override silently forfeits the ponder move (deployment regime)
`src/search.rs:1926-1972`, `src/uci.rs:704-721`. Helpers return only `(nodes, mv, score, depth)` — no PV — and the vote returns a bare move without updating `pv_table[0]`/last_score. When the vote picks a helper's move (exactly when voting adds value), uci.rs's `pv_consistent` check fails **by construction**, so `bestmove` is emitted with **no ponder** — the lichess deployment (Threads>1 + ponder) forfeits its entire ponder benefit on every vote-override move, plus GUI info/bestmove desync. Also missing: the 5-engine proven-win/proven-loss vote guards, helper-move validation against root_legal, and the `len()<=1` early-out prefers main's depth-0 fallback over a depth>0 helper result.
**Change:** select a best *thread* (proven-win → highest score; never switch to proven-loss or pv_len<2), return per-thread `(move, score, depth, pv[0..2])`, adopt the winner's 2-move PV into `si` so the ponder emit stays armed. Cheap ponder-only alternative: probe shared TT after the winning move. **Test:** OB cannot measure this — local cutechess RR with ponder per TM-class methodology, then [-2,1] Threads=1 non-regression. Novelty: new.

### 6. Helper threads never seeded with `trans_corr` — the one corrhist table missing from the copy list
`src/search.rs:1676-1678` (seed list), field at :904, cleared at :1162, read at :1436 with weight `CORR_W_TRANS=74` (~12% of corrhist weight). Seeding landed 2026-06-12 (T2.1, #1970 H1 +4.6); trans_corr landed 2026-06-26 (#2313 H1 +5.1) without touching `create_helper_info` — classic add-table-forget-the-seed. Helpers (respawned per `go`) run every search with a zeroed transition component while main runs warm, so threads compute systematically different corrected evals and push divergent search results into the shared TT.
**Change:** `helper.trans_corr.copy_from_slice(&main.trans_corr[..]);` at :1678. **Test:** bench-identical at Threads=1; [-2,1] at T>1 or bundle with another SMP change (standard OB SPRTs run Threads=1 and cannot see it). Novelty: new.

### 7. Incomplete `is_decisive` migration: mate-only bounds (±MATE_IN_MAX_PLY) admit TB-band scores at ~10 sites
(Merged: whole-node #3 + move-loop #6 + tt #17.) TB scores (`TB_WIN − ply` ∈ [28672, 28800]) pass every `|s| < MATE_IN_MAX_PLY` (28872) guard — the exact trap `tt.rs:599-606` documents and that T2.3/#1941 fixed for multicut/corrhist/ProbCut but not for:
- **NMP fail-high clamp** `src/search.rs:3702` — returns unproven TB-wins (SF/Reckless skip the cutoff for win-scores; minimal fix: `is_decisive(null_score) → beta`; also the strict `>` misses a mate at exactly ply 128).
- **Move-loop prune gates** :3988-89 (LMP), :4006 (SEE-cap), :4031-32 (SEE-quiet), :4190-91 (futility), :4219 (BNFP), SE gate :4070, QS quiet-evasion skip :5118 — prunes stay live while best_score is a TB-loss (when the search should widen). Consensus fix is the **best_score side** (`> -(TB_WIN-200)` / is_loss); the beta side is Coda's own #2321 extension.
- **TT score shaping** — dampening :3317-3323 (a TB win dampens to ~21.6k, permanently *leaving* the ply-adjust band and destroying distance-to-win), near-miss :3327-3343, FH blend :4907-4916, both QS blends :5261-5265/:5395-5400, and the QS stand-pat refinement :5234 (SF gates its equivalent on `!is_decisive`).
Reachable mostly with SyzygyPath set (lichess/CCRL) — but `downgrade_50mr_mate` injects TB-band scores even without TB, so rarely at OB too.
**Change:** replace the mate-only bounds with `is_decisive()`/is_loss-style helpers (already exist) across all sites in one sweep. **Test:** bench-identical without TB; [-2,1] non-regression + local 5-man EGTB RR per the local-rr skill (F3 precedent). Novelty: new; #2321/#2412/#2418 are the supportive same-family priors.

### 8. Next-iteration hard-window estimate uses the stale pre-verification `elapsed` snapshot (half-applied A4 fix)
`src/search.rs:2852` uses `elapsed` from :2464 (taken before the info print and the 50–150ms forced-move verification at :2660); the A4 audit fix added `elapsed_now` (:2833) but applied it only to the soft check (:2835). On common non-firing verification iterations the check grants ~50–150ms of unwarranted slack, green-lighting an unaffordable iteration that dies mid-way (partial iteration discarded). (Merged: skeleton #22 + driver #55.)
**Change:** use `elapsed_now` in the :2850-2854 check; consider also breaking when `elapsed_now >= effective_hard`. **Test:** one-liner, TM-hygiene class — bundle into the next TM pass at [-2,1] (same class as #1949), with a quick tm_pattern_inspect mechanism check. Novelty: new.

### 9. Rare-path cap-guard bundle: in-check eval at ply/depth caps + QS missing MAX_PLY guard
(Merged: skeleton #24 + qsearch #35 + skeleton #27.)
- `src/search.rs:2985-2987` (negamax `ply >= MAX_PLY`) and :4986-4988 (QS `qs_depth >= 32`) return raw NNUE eval **even in check** — the only places Coda ever evaluates an in-check position (treated as undefined everywhere else: `static_evals=-INFINITY`). SF/Obsidian/Berserk/PlentyChess all return draw/0 in check at their caps.
- QS has **no ply ≥ MAX_PLY entry guard**: entered at ply ≤ 128 (ProbCut passes ply+1) and recursing 32 more, the evasion checkmate return `-MATE_SCORE + ply` (:5169) can emit |score| ∈ [28841, 28871] — inside the "empty" TB band that ~30 guards now rely on (commit 077f727). Reachability of deep QS evasion chains is empirically proven (lichess ASuoXT9f clamp comment at :5087-5091).
**Change:** at both caps return 0 when `board.checkers() != 0`; add a QS entry `ply >= MAX_PLY → return (in_check ? 0 : eval)` guard mirroring negamax/SF/Reckless. **Test:** near-zero Elo (cf. #2416: a mechanism firing 1600×/game still measured 0.0 at 412k games) — ship as one [-2,1] correctness bundle. Novelty: new.

---

## P1 — High-EV Elo experiments

### 1. Merge the lost H1: LMR cut-node bump (#2065, +1.2 ±0.9 at 146k games, never merged)
`src/search.rs:4321-4324`. Main's "cut node" LMR term is actually `if !is_pv && move_count > 1 { reduction += 1; }` with a vacuously-true conjunct — cut nodes and all nodes are indistinguishable in LMR, despite `cut_node` being threaded everywhere else. Branch `origin/experiment/lmr-cutnode-bump` (e616393: `reduction += if cut_node { tp(&LMR_CUTNODE_BUMP) + (tt_move == NO_MOVE) as i32 } else { 1 }`) **locked H1 at [0,3]** (OB #2065, verified against the OB page) but was dropped on a conflicting audit-doc note that git timestamps prove was an early interim read (5.7h in) predating the H1 lock. SF (:1220) and Reckless carry exactly this bump + no-TT-move kicker.
**Change:** resolve the record conflict in experiments.md, rebase e616393 onto current main (two trunk retunes + fractional-LMR landed since), re-bench, re-run [0,3]. Delete the dead conjunct and fix the mislabeled comment while there. Novelty: retest-candidate (lost H1 — highest EV/risk in this report). Prior: experiments.md:63 / OB test 2065.

### 2. QS gap-aware futility: prune non-material-winning captures when `eval + margin <= alpha`
`src/search.rs:5310-5329`. Coda's only QS futility is the victim-value delta prune + static SEE gate; there is **no clause pruning equal trades when stand-pat is hopelessly below alpha** (QxQ delta value clears alpha almost always). 6/6 strongest engines have the gap-aware form (SF `see_ge(move, alpha - futilityBase)`; Obsidian/Berserk `futility <= alpha && !seeGe(move,1)`; Reckless scales its SEE threshold by `(alpha-eval)/8`). Not covered by the 2026-06-24/25 QS audit (which produced +6.6 and +2.6 in this family).
**Change:** after the delta prune, before the SEE gate: `let fut = stand_pat + tp(&QS_FP_MARGIN); if fut <= alpha && best_score > -(MATE_IN_MAX_PLY) && !see_ge(board, mv, 1) { best_score = best_score.max(fut); continue; }` (margin tunable ~60–330; Berserk 63, SF 328). **Test:** [0,3]. Novelty: new. Bundles naturally with P1.12 (fail-soft delta prune).

### 3. Correction history trains on the PRE-correction error (open-loop integrator; 5/6 train on the residual)
`src/search.rs:4875-4893` (gates + call), err at :1473. (Merged: skeleton #31 + ordering #45.) Coda updates all five corrhist tables with `best_score - scaled_eval` (pre-correction); SF/Reckless/Obsidian/PlentyChess/Alexandria all train on `bestValue - correctedStaticEval`, making corrhist a negative-feedback loop that self-normalizes across correlated sources. Coda's open-loop form winds every table toward the ±1024 cap for *any* persistent same-sign bias regardless of magnitude (saturation ≈ 50cp with current weights), feeding RFP/futility/LMR/SE everywhere. Berserk trains pre-correction but pairs it with an EMA update and a single table — no reference pairs gravity-integrator with pre-correction error. The 2026-06-26 corrhist deep-audit blessed this site via wrong variable identification, so it is genuinely unexamined, not calibrated.
**Change:** pass `static_eval` (corrected) instead of `scaled_eval` at :4893 **and** in the :4875-4878 direction gates (references are consistent on both). **Test:** [0,3]; expect the CORR_* basin to shift — plan a focused corr-cluster **retune-on-branch**. Novelty: new. Direction not guaranteed; magnitude +0-3 class.

### 4. Bank aborted-iteration root results — including completed aspiration fail-highs
`src/search.rs:2360-2406` (aspiration loop), :2389-2397 (stop restore), :4620-4628 (root PV write). (Merged: skeleton #30 + driver #56; #57 as enabler.) On any mid-iteration stop Coda restores the previous iteration's stable PV and discards everything the deepest, most expensive iteration learned — including a **fully-searched new best root move** (pv_table[0] is internally consistent at abort; pv_len[0]==0 cleanly signals "nothing completed") and a **proven root fail-high move** (the loop widens beta at :2373-2379 without capturing pv_table[0][0]; negamax wipes pv_len[0] at entry, so a stop during the re-search reverts to the older, known-worse move). All 6 references keep partial-iteration root results usable (SF sorts rootMoves before the stop-break; Obsidian: "usable at any time"). Costs exactly at time pressure, Coda's diagnosed weak regime (LTC/TM).
**Change:** (1) on abort, if `pv_len[0] > 0` take `best_move = pv_table[0][0]` validated against root_legal (Reckless-style unproven-loss guard), only restoring stable_pv when nothing completed; (2) inside the aspiration loop, snapshot the fail-high move + PV before widening. Preserve best_move/PV pairing (ponder-consistency, lichess oeZ7KRUt class). **Test:** [0,3], most visible at LTC. Novelty: new. The full persistent **RootMoves array** (driver #57: per-move score/prev_score/avg/pv, stable-sorted per aspiration attempt — 5/6 consensus) is the architecture investment that also enables the average-score window, SMP thread selection (P0.5), and MultiPV; justify it by these consumers rather than direct Elo.

### 5. In-check QS searches every capture evasion with zero SEE pruning
`src/search.rs:5102-5165`. The evasion loop prunes only quiet evasions; capture evasions always get make_move + NNUE push + recursive QS — no SEE gate, no budget. 5/6 references SEE-prune capture evasions once best_score is no longer mated (their QS prune block gates on `!is_loss(bestValue)`, not `!inCheck`). #1946 (H1 +3.1) fixed the same audit seam but deliberately left "capture evasions always searched".
**Change:** after the quiet-skip: `if ev_is_cap && best_score > -(MATE_IN_MAX_PLY) && !see_ge(board, mv, 0) { continue; }` (Berserk threshold 0; SF −73). Checkmate detection unaffected (needs one searched move). **Test:** [0,3]; magnitude likely low end of +1-2 (evasion sets are small) — consider bundling an evasion move-count cap (Reckless/Obsidian) if solo signal is weak. Novelty: new.

### 6. Remove `!in_check` from the move-loop SEE prunes (keep it on LMP/futility/BNFP)
`src/search.rs:4008` (SEE-cap), :4031` (SEE-quiet). All 6 references run SEE pruning at in-check nodes (Reckless is the sharpest analog: explicit `!in_check` on LMP/FP/BNFP, SEE prune deliberately ungated); SEE is eval-independent so the check-gate rationale doesn't apply. Mechanically safe: `best_score > -(MATE_IN_MAX_PLY)` already prevents pruning before one evasion has been searched. Same gate-class removal as #1943 se-in-check (H1 +5.1); the 2026-06-23 SEE audit's "confirmed correct" checkmark on this gate had no cross-engine evidence and is contradicted by it.
**Change:** drop `!in_check` from the two SEE prune gates only. **Test:** [0,3]. Novelty: new.

### 7. QS TT stores hardcode `tt_pv=false` — sticky PV flag erased on cross-generation overwrites
`src/search.rs:5188` (evasion store), :5392 (capture-loop store); tt.rs same-key path :476-489 repacks the caller's bit. At the start of every new search, the first QS visit to a prior-generation `tt_pv=true` entry erases the sticky marker (PV QS nodes never TT-cutoff, so PV leaf regions always fall through to the store). Preserving the bit in QS final stores is 3-of-3 top-engine consensus (SF/Reckless/Obsidian `pvHit = ttHit && tte->is_pv()`); the flag feeds LMR −1 / RFP skip / SE margins (+4.5→+8.5 when introduced, #160/#173) and the identical ProbCut `tt_pv:false` defect was already fixed (H1, OB #428). The stand-pat fail-high store (:5257) is *not* a defect (gated `!tt_hit`; SF passes false there too).
**Change:** pass `tt_hit && tt_entry.tt_pv` at the evasion and capture-loop stores (SF/Obsidian form); optionally OR in `qs_is_pv` as a Reckless-form variant. **Test:** [0,3]; expect a node increase (ProbCut fix gave +7% nodes) — retune-on-branch candidate per the #160/#173 precedent. Novelty: new.

### 8. TT cutoff depth condition symmetric in bound direction (top-3 demand +1 ply for fail-high)
`src/search.rs:3177` (main gate), :3327-3343 (near-miss both directions behind 80cp). SF (`ttData.depth > depth - (ttData.value <= beta)`), Reckless, Obsidian, PlentyChess all require one extra ply of TT depth for fail-high cutoffs; Coda is more permissive than the top 3 in *both* directions (symmetric gate + margin-gated near-miss ply both ways). Note the corrected picture: references accept fail-low at `tt_depth >= depth`, not depth−1, and Coda's cut_node==score_above_beta guard (+2.4, #149) already restricts fail-highs somewhat.
**Change:** `tt_depth > depth - (tt_score < beta) as i32` on the main gate; restrict/re-tune the near-miss to the fail-high direction (the else-if fall-through changes meaning). **Test:** [0,3]; tree-shape change → **retune-on-branch candidate**. Risk: heavy SPRT calibration around the current symmetric shape. Novelty: new.

### 9. Non-capture promotions take the QUIET history path; the caphist bin the picker reads for them is never written
`src/search.rs:4638` (split on `!is_cap`), :4277 (tracking excludes promos), :4707 (positional malus exclusion); movepicker.rs:906-958; movegen.rs:138-144. (Merged: ordering #44 + move-loop #5.) A push-promotion cutoff bonuses main/cont/pawn history — tables the picker never reads for promos (they're scored in the captures stream via `capture[piece][to][0]`, a bin provably never written) — pollutes piece-agnostic main-hist from/to cells, and breaks the malus loop's `quiets_count-1` positional exclusion so the last genuinely-failed quiet escapes its malus (the capture-malus loop at :4798 already does the exclusion conditionally — an internal inconsistency, not a calibrated choice). References route promos through the noisy path into the bin their pickers read.
**Change:** `is_noisy = is_cap || is_promo` at :4638 and the TT-collapse site :3291; route noisy cutoffs through the caphist bonus with ct=0 for push-promos (drop the `captured_pt != NO_PIECE_TYPE` guard); add promos to the noisy-tried list; make the quiet-malus exclusion explicit (`if q == mv { continue; }` — correct under saturation too). **Test:** [0,3] per the #1670 precedent (+2.5, same consistency class). Novelty: new.

### 10. `best_move` recorded without the `score > alpha` gate — fail-low stores plant a "least-bad" TT move (direction uncertain)
`src/search.rs:4615-4617` (negamax), :5158/:5368 (QS). (Merged: tt #16 + qsearch #47.) All 6 references assign bestMove only on alpha raise, so their UPPER stores pass a null move and the TT preserves the deeper iteration's proven move; Coda's negamax stores are never NO_MOVE, making the merged #2121 tt.rs preserve-on-NO_MOVE logic dead code from negamax, and a planted UPPER tt_move also suppresses IIR (`tt_move == NO_MOVE` gate, :3750). **Contra-priors are real:** #1931 (H0 −1.6: "the tt_move of an UPPER entry ... evidently carries useful ordering signal"), #1945, #1660 — the fail-low-signal family has H0'd three consensus ports. One verifier disputed whether tt.rs move-preservation covers all store paths — confirm the NO_MOVE-same-key preserve (tt.rs:480-484, #2121) is reached before relying on it.
**Change:** move `best_move = mv;` inside the `score > alpha` block (negamax first; QS as a separate probe). **Test:** [-1.5,1.5] (uncertain direction) or [0,3]; if H0, log next to #1931/#1945 as deliberate divergence to close the axis. Novelty: new.

### 11. Main-search evasion ordering: capture band crossable by history (references make it uncrossable)
`src/movepicker.rs:787-792`. Evasion captures get `10000 + mvv_lva + capt_hist` vs full quiet-history sums spanning ±80k — a hot quiet outranks a fresh capture of the checker, and a negative-captHist capture sinks below quiets. SF (`+ (1<<28)`), Berserk (`1e7 +`), Obsidian (separate captures stage) all enforce strict captures-first. Scope correction from verification: QS evasions are already uncrossable (flat −1M quiets) — main-search evasions only.
**Change:** raise the capture band offset to uncrossable (e.g. `1 << 20`), keeping mvv+captHist intra-band; preserve the C8 #26 capture-promo handling. **Test:** [0,3]. Magnitude priors small (evasion sets 2–8 moves; prior evasion-ordering experiments ~0). Novelty: new.

### 12. Smaller QS/loop one-liners worth bundling
- **QS delta prune not fail-soft** (`src/search.rs:5318-5321`): raise `best_score = best_score.max(fut)` when pruning, matching all 5 value-prune references — sounder UPPER bounds; note Coda's 2.15×-victim fut pushes bounds near alpha, consider a 1×-victim variant. Bundle with P1.2, [0,3]. (qsearch #43)
- **QS skips for non-queen push-underpromotions** (movegen.rs:138-144 + exemptions at search.rs:5305/:5311): all four promo pieces are generated and exempt from budget+delta; SF/Reckless/PlentyChess generate queen-only pushes in noisy movegen. Least-invasive: skip non-queen promos in the QS loop **only when the destination is empty and non-EP** (keep capture-promos and evasions). [0,3]. (qsearch #52)
- **TT-cutoff cont-hist malus lacks the `(ss-1)->moveCount < 4` gate** (search.rs:3229-3248): SF/Obsidian/Alexandria (the port source) all gate it; needs a small per-ply move_count stack (#1961 built the same plumbing, H0 for a *new* signal — this restricts an existing noisy write, distinguishable but the neighborhood has 5 adjacent H0s). [0,3] STC; an H0 closes the fifth and last axis. (ordering #34)
- **QS fail-high blends gated to non-PV** (search.rs:5258/:5393): all 6 references blend regardless of node type; ≤1 Elo, [-1.5,1.5] bundle. (qsearch #46)

---

## P2 — NPS / efficiency

### 1. QS dispatch after the negamax preamble: TT probe, draw checks, cuckoo, prefetch run twice; nodes double-counted
`src/search.rs:3350` (dispatch) vs quiescence :4968-5024. Every depth≤0 negamax entry (~14% of calls) pays the full preamble — including the enemy_attacks/xray computation (~20-30 magic lookups, unused before dispatch) and a full 5-slot TT probe — then QS repeats draw checks (provably dead on first entry), prefetch, cuckoo, a second TT probe of the same hash, and a second `info.nodes += 1` (~10% node-count inflation at the boundary, which also leaks into the TM node-fraction signal). SF/Reckless dive to qsearch as the first thing in search().
**Change:** hoist the depth≤0 dispatch to the top of negamax — *after* the ply≥MAX_PLY guard; validate the two deliberate semantic deltas (boundary nodes lose the interior TB probe — SF-consistent — and mate-distance pruning). QS's TT cutoff (depth ≥ −1) is a superset of the depth-0 requirement. **Test:** [-2,1]; ~0.5-1% NPS. Novelty: new. (qsearch #49)

### 2. Quiet futility runs AFTER SEE-quiet pruning (5/6 run cheap futility first)
`src/search.rs:4028-4040` (SEE per-move continue) vs :4188-4210 (terminal futility). At futile nodes the loop keeps drawing quiets and re-running see_ge (pin-mask setup) until one survives SEE and finally reaches the block that would have terminated all remaining quiets. All inputs available before either block; the analog LMP reorder was #2283 H1 +0.6. Verifier sizing: <0.5% NPS (terminal futility is the rarest quiet pruner) plus a small pruning-shape change (earlier skip_quiets bypasses the direct-check/hist carve-outs).
**Change:** move the futility block above SEE-quiet. **Test:** [0,3] (not a pure reorder). (move-loop #10)

### 3. SEE-cap prune and BNFP re-run `see_ge` on captures the picker's good/bad split already determines
`src/search.rs:4008-4014` / :4221, movepicker.rs:531-534. By SEE monotonicity, a good-stage capture with `capt_hist <= 18*cap_margin` (~2141×depth — the overwhelming majority) provably passes the SEE-cap prune; BNFP's `see_ge(mv,0)` is implied by stage for the convenient capt_hist sign. Caveats from verification: SF/Reckless also tolerate the SEE-cap redundancy (NPS micro-opt, not a structural gap), and capt_hist drifts within a node, so an exact skip needs the picker to expose its generation-time verdict. The clean consensus-sourced piece is **Reckless's BNFP stage gate** (`stage == BadNoisy` instead of see_ge).
**Change:** expose the picker stage; BNFP stage gate as its own [0,3]; provably-dead-call skip as [-2,1] NPS. (move-loop #11)

### 4. NMP undefended-count precompute stranded before RFP and missing the cut_node/nmp_min_ply gates
`src/search.rs:3528-3544`. Computes a full own-side attack map before the razor/RFP block for a value only consumed by NMP (which requires cut_node/nmp_min_ply/FEAT_NMP); every RFP-cutoff node pays it for nothing, and the all-node half of gate-passing ZW nodes can never consume it. Stale-order artifact of the #1882 RFP-before-NMP reorder. Behavior-identical move (<0.5% NPS realistic).
**Change:** move the block to just above nmp_threat_margin (~:3645) and add `&& cut_node && ply >= info.nmp_min_ply` to the cheap gate. **Test:** bench-nodes identical; bundle into a [-2,1]. (whole-node #1)

### 5. LMP evaluates `gives_direct_check` before the movecount test
`src/search.rs:3985-3999`. At depth 1–3 every quiet non-promo pays a gives_direct_check call even when the prune cannot fire (first move, moves under the limit, TT move), and a surviving move may pay it again in the futility carve (:4207, correctly ordered there). Semantics-preserving reorder, node-count identical.
**Change:** hoist the carve inside `move_count > lmp_limit`; optionally memoize the check shared with futility at depth<4. **Test:** bundle into a [-2,1]; <0.5% NPS. (move-loop #7)

### 6. Helper threads respawned per `go` instead of a persistent pool (SMP-only)
`src/search.rs:1888-1929`, :1628-1680. Per-move cost at Threads>1: N−1 spawns, ~260KB corrhist + history memcpy, fresh ~13MB pawn_hist allocation (unseeded), cold Finny/refresh tables; helpers' own learned deltas discarded each move. All 6 references keep persistent pools with per-thread state surviving across moves. Mitigations noted: the per-go copy-from-main was a conscious T2.1 design; diversity loss is arguable.
**Change:** keep helper SearchInfos alive across `go`, wake per search, age histories like main. **Test:** zero effect at Threads=1; validate via local multi-thread RR. Moderate implementation effort. (driver #59)

### 7. TT prefetch is a compile-time no-op on aarch64
`src/tt.rs:512-519` — x86_64-only `_mm_prefetch`; all 7 search.rs prefetch call sites silently do nothing on ARM (first-class platform). **Scope correction:** do *not* port the threats.rs prefetches (threat-side prefetch is a known-negative lever: #719 −4.4, #721 −8.1, 2026-06-14 ladder closure).
**Change:** add `#[cfg(target_arch="aarch64")] asm!("prfm pldl1keep, [{0}]", ...)` to `TT::prefetch` only. **Test:** cannot be OB-gated — local M-series uncontended NPS A/B only (per NPS-measurement discipline). No rating-bearing ARM deployment today → low. (tt #13)

### 8. Driver hygiene with measurement value: see P4 items 3–4 (bench global_nodes, seldepth) — zero Elo, no SPRT.

---

## P3 — Retest candidates (prior result quoted; why a retry might pay now)

### 1. TT-bound refinement of the pruning eval (razor + RFP core) — the consensus two-variable form has never actually been tested
`src/search.rs:3371-3416` (static_eval), consumers :3562 (razor), :3599 (RFP), :3651/:3667 (NMP). (Merged: whole-node #2 + tt #9 + skeleton #25 — found independently by three clusters.) All 6 references tighten a *separate* pruning-eval variable with the TT score when the bound direction agrees (no depth gate), keeping staticEval pure for improving/corrhist/TT-store. **Priors:** 2026-03-12 "TT score refines static eval for pruning" −77.1 @ 174g (experiments.md:812) — but it added a non-consensus `ttDepth >= depth-3` gate, **replaced the static eval itself** (polluting improving + corrhist), and its rationale ("SF only adjusts the improving flag") is verifiably wrong (SF search.cpp:749-752 replaces eval); #1019 TT-blended RFP eval H0 −13.4 (blend, no bound check); 2026-03-31 TT-adjusted NMP eval, 300g noise. **Why now:** the exact QS analog banked **+6.5 H1** (fix-qs-tt-stand-pat, post correction-stack), and the consensus form with correct guards has never been run. **Load-bearing implementation notes from verification:** (a) Coda's eval-seed TT stubs store `score=-INFINITY, flag=UPPER`, which trivially passes the UPPER test — the QS-style `|tt_score| < MATE_IN_MAX_PLY` and halfmove guards are mandatory, and the −77 smells like exactly this class of leak; (b) scope to **razor + RFP only** first (the true 6/6 core — SF's NMP/futility/ProbCut use *unrefined* staticEval; only Reckless refines NMP), with NMP as a separate variant.
**Test:** [0,3]; retune-on-branch fallback given three prior fails in the family. Priority: highest-EV experiment in this report after P1.1.

### 2. LMR `tt_depth >= depth` reduce-less term — re-isolation of a bundled H0
`src/search.rs:4308-4431` (no tt_entry.depth use in either LMR block). Obsidian (:1079) and Berserk (:729-730) apply it unconditionally; SF/Reckless inside their ttPv term (4/6). **Prior:** #876 lmr-tt-bound-informs H0 −2.7 @ 15k (2026-04-30, class-closure) — but it *bundled* tt_depth with a tt_score≤alpha term later shown independently negative (#2227 H0 −1.7) and used the old-Reckless reduce-MORE-when-shallow complement, not the one-line reduce-less form. Baseline has moved ~+116 Elo since.
**Change:** `if tt_hit && tt_entry.depth as i32 >= depth { reduction -= 1; }` (Obsidian form). **Test:** [0,3]; modest prior given the June audit's LMR hit-rate.

### 3. Aspiration window centered on a running average — clean half of a bundled H0
`src/search.rs:2352` (`let avg = prev_score;` — center and delta² both use the raw last score). SF/Reckless/Berserk/Alexandria center on `(avg+value)/2`. **Prior:** #1935 atlas/aspiration-pair H0 −0.9 ±1.9 @ 37.8k — T2.8 (this) bundled with T2.7 (fail-high reset/cap that weakened an individually-SPRT'd mechanism); the experiments.md note itself says "if revisited, test halves separately". **Test:** T2.8 alone, [0,3] STC; expected ≤ +1.

### 4. QS double-SEE dedup (picker partition + loop QS_SEE_THRESHOLD gate)
`src/search.rs:5327` + movepicker.rs:531-534. (Merged: ordering #40 + qsearch #51.) Every picked QS capture pays see_ge twice; SF's QS runs exactly one SEE. **Prior:** 2026-03-12 "QS Skip Bad Captures (remove double SEE)" flat at 1366g — "NPS gain ... negligible" — but that bundled dropping bad captures entirely, and see_ge has since gained the pin-mask setup (67a3b24, 2026-06-23), making the wasted call materially pricier. A behavior-preserving cached-verdict variant is untested. **Test:** [-2,1] NPS-only; measure NPS uncontended first. Expectation: small; the project's record on bench-identical recompute-elimination is flat.

### 5. Root-score eval optimism — tried once, untuned, H0
No trace in src/ today, but **#671 p1-optimism-c8-retry H0 −7.1 @ 4268g** ("could iterate via K1/K2 focused SPSA on-branch"). SF/Reckless/PlentyChess have it (3/6 — including both engines above Coda's cluster); Reckless even shares root stats across threads for it. The legitimate retry is port + **focused SPSA retune-on-branch** (eval-blend features shift the whole threshold landscape), never another untuned probe. Medium risk (nnue.rs integration); low priority. (driver #58)

### 6. FH blending applied after the TT store (SF/Stormphrax/Caissa blend before)
`src/search.rs:4841-4855` store then :4907-4915 blend. Unanimous among feature-havers, but: direct QS analog #2136 H0 −1.9 ("near-zero practical impact"), and Coda's probe-side TT_DAMP (+22.1 added / −43 removed) already dampens the replay path — a stored blend risks double-dampening and weaker LOWER bounds (the ProbCut store-raw H1 precedent). At most a [-1.5,1.5] probe with TT_DAMP interaction considered; gain unlikely. (tt #18)

### 7. LMR negative reduction / mini-extension — decisively dead once, one thing changed
**Prior:** #865 lmr-pv-neg-reduction H0 **−8.0** ("a clear blunder"), part of the formally-closed 8-H0 LMR carve-out class; unmerged branch `experiment/lmr-mini-extension` (b4b5a3f, the exact Reckless shape) was built and abandoned untested. Only reason to revisit: #2192 lmr-fractional (H1) changed how continuous negative terms sum. If ever retried, rebase b4b5a3f *after* the pending fractional-LMR cluster SPSA and quote #865. Low. (lmr-ext #38)

---

## P4 — Code quality / hygiene (no SPRT unless noted)

1. **xray-SE margin comments state the inverted mechanism** (`src/search.rs:4076-4078`, tunables :408-412). The bonus *lowers* singular_beta → **stricter** singularity → *fewer* extensions on xray TT moves — the opposite of both comments. Shipped behavior is empirically fine (#604 H1 +1.1; SPSA pushes the value up, away from the 0 floor, so the sign-flip probe is contra-indicated). Rewrite the two comments to prevent a future wrong-direction "fix". (lmr-ext #20)
2. **NMP post-capture R+1 comment cites a consensus that no longer exists** (`src/search.rs:3663`). SF's R is flat; Obsidian keys on the *current* node's ttMoveNoisy. The term itself is empirically validated (#195 H1 +3.5 with retune; removal #1067 H0 −2.6) and the ttMoveNoisy port is dead ×4 (#732/#754/#768/**#2270, six days ago**). Action: comment fix only — cite #195/#1067 instead of "consensus". (whole-node #0)
3. **bench never resets `global_nodes` despite the comment claiming it does** (`src/search.rs:2109-2110` vs bench_inner :5574-5577): every bench info line after position 1 prints cumulative nodes and garbage NPS; final `Bench:` total (and OB verification) unaffected. Add the store to the per-position reset; verify bench nodes unchanged. (driver #60)
4. **seldepth never reset per iteration** (`src/search.rs:2111`): reported seldepth is a whole-search running max; all references reset per ID iteration. Move the reset inside the ID loop in search() and search_helper(). (driver #61)
5. **50-move-rule checkmate precedence — KNOWN DEAD, do not re-run.** Three finders re-derived it (skeleton ×2, qsearch #37); the exact fix was SPRT'd as **#2416: H0, −0.4 ±0.5, LLR −2.99 locked at 412,340 games** (2026-07-01, one of the tightest CIs on record) despite the instrumented mechanism firing ~1,600×/game — the search finds those mates via other lines. Only a merge-policy question remains (it would pass [-2,1] if rule-correctness is valued intrinsically); zero strength case.

---

## Top 5 to run first (expected Elo ÷ implementation risk)

1. **Rebase & re-run the lost H1: LMR cut-node bump #2065** (P1.1) — already locked H1 +1.2 at [0,3] over 146k games; the code exists on `origin/experiment/lmr-cutnode-bump`; only a rebase + re-bench + revalidation stands between it and main.
2. **Fix `cut_node=false` on the non-PV first move** (P0.1) — a one-token, 6/6-consensus bug disabling NMP/IIR/TT-cutoffs on the hottest path in the tree; [-2,1] with retune-on-branch fallback.
3. **QS gap-aware futility clause** (P1.2) — ~6-line port of a 6/6-consensus prune into the highest-volume loop in the engine, in the exact audit family that just banked +6.6 and +2.6; [0,3].
4. **TT-bound refinement of the pruning eval, razor+RFP scope** (P3.1) — the consensus form was never actually tested (all three priors were structurally different); the QS half of the same audit item banked +6.5; the failure-mode guards are now identified; [0,3].
5. **ProbCut `moved_piece_stack` fix** (P0.2) — two lines, [-2,1] confident-correctness, stops active pollution of the shared cont-hist tables and unblocks every future cont-hist-sensitive experiment (the stated rationale for merging the NMP-sentinel analog).

Honorable mentions if fleet allows: in-check QS capture-evasion SEE gate (P1.5), aborted-iteration banking (P1.4), corrhist residual training + CORR retune (P1.3).

## Honest assessment

This pass found genuinely new material, but the distribution is telling. There are three or four **real, traced bugs with plausible Elo** (first-move cut_node, ProbCut cont-hist stack, ZW inverted window, the lost #2065 merge) and two or three **high-quality untested consensus gaps** (QS gap-aware futility, aborted-iteration banking, corrhist residual training, TT-bound pruning-eval refinement done right) — realistically 5–15 self-play Elo across the top items if hit rates match the June waves. But the long tail has changed character: roughly a third of the confirmed findings are deployment-only (TB/SMP/ponder/ARM/`go nodes`) correctness that OB cannot even measure, another chunk are sub-1-Elo NPS reorders, and several "new" findings turned out on adversarial verification to be re-isolations of tested territory or re-derivations of experiments closed within the last week (#2416 was independently rediscovered three times). The interior search — move-loop pruning, LMR terms, history maluses — is close to mined out; the remaining leverage has visibly migrated to the **driver/root layer** (root-move persistence, SMP selection, abort banking) and to **deployment-regime correctness**, which is consistent with the standing diagnosis that Coda's gap to the top 5 is TM + LTC-scaling rather than tree shape. Not at the floor yet, but the next 100 Elo will not come from another pass like this one; after the top items here are drained, the driver-layer rearchitecture (RootMoves + thread selection) and TM/ponder work look like the better dig sites.
